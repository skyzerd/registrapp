import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'registrapp',
  appName: 'RegistrAPP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
